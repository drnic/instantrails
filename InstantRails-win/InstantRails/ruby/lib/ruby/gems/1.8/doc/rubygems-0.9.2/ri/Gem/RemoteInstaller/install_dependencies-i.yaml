--- !ruby/object:RI::MethodDescription 
aliases: []

block_params: 
comment: 
- !ruby/struct:SM::Flow::P 
  body: Install all the given dependencies. Returns an array of Gem::Specification objects, one for each dependency installed.
- !ruby/struct:SM::Flow::P 
  body: "TODO: For now, we recursively install, but this is not the right way to do things (e.g. if a package fails to download, we shouldn't install anything)."
full_name: Gem::RemoteInstaller#install_dependencies
is_singleton: false
name: install_dependencies
params: (dependencies, force, install_dir)
visibility: public
